# 潮州方言Agent驱动伴学网页教育系统 — 架构设计框架

---

## 一、系统架构总览

### 1.1 架构分层

系统采用**四层分离架构**，从上到下依次为：前端展示层、后端服务层、AI Agent 编排层、数据持久层。层间通过明确的 API 合约解耦，AI Agent 编排层作为独立领域嵌入后端服务层内部，形成"大脑"角色。

```
┌─────────────────────────────────────────────────────────────────┐
│                        前端展示层 (Browser/WebView)                │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐           │
│  │ 学习中心  │ │ Agent对话 │ │ 数据看板  │ │ 用户中心  │           │
│  │ (Svelte)  │ │ (Svelte)  │ │(ECharts) │ │ (Svelte)  │           │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘           │
│                       ▲                                          │
│                  HTTPS / WSS (Socket.IO)                         │
├───────────────────────┼─────────────────────────────────────────┤
│                       ▼          后端服务层 (Node.js/FastAPI)     │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐           │
│  │API 网关   │ │用户服务  │ │学习服务  │ │数据分析  │           │
│  │(Kong/Nginx)│ │(Auth/CRUD)│ │(课程/练习)│ │(统计/评估)│          │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘           │
│  ┌──────────────────────────────────────────────────┐           │
│  │              AI Agent 编排引擎                     │           │
│  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐   │           │
│  │  │意图识别│ │记忆管理│ │任务规划│ │工具调用│ │策略决策│   │           │
│  │  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘   │           │
│  └────────────────────┬─────────────────────────────┘           │
├───────────────────────┼─────────────────────────────────────────┤
│                       ▼          国产大模型接入层                  │
│  ┌──────────────────────────────────────────────────┐           │
│  │          模型路由器 (Model Router)                 │           │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐          │           │
│  │  │ DeepSeek │ │ 通义千问  │ │ 智谱GLM  │  ...     │           │
│  │  │ (推理)    │ │ (长文/多模态)│ │(Agent)   │          │           │
│  │  └──────────┘ └──────────┘ └──────────┘          │           │
│  └────────────────────┬─────────────────────────────┘           │
├───────────────────────┼─────────────────────────────────────────┤
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐            │
│  │  PostgreSQL  │ │    Redis     │ │  MinIO/OSS   │            │
│  │ (业务/用户)  │ │ (缓存/Session)│ │ (音频/媒体)  │            │
│  └──────────────┘ └──────────────┘ └──────────────┘            │
│  ┌──────────────────────────────────────────────────┐           │
│  │      向量数据库 (Milvus/Qdrant)                    │           │
│  │      (方言知识库 / 学习材料语义索引)                │           │
│  └──────────────────────────────────────────────────┘           │
│                      数据持久层                                   │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 数据流向总述

| 流向路径 | 说明 |
|---------|------|
| 用户操作 → 前端 → API网关 → 业务服务 → 数据库 | 标准CRUD流程 |
| 用户输入(语音/文本) → 前端 → WSS → Agent引擎 → 大模型 → 响应 | Agent实时对话链 |
| 定时任务/Cron → 数据分析服务 → 聚合计算 → 写入统计表 | 离线分析链路 |
| Admin上传 → 内容管理 → 向量化 → 存入向量库 | 知识库构建链路 |
| 学习行为 → 埋点收集 → Kafka(可选) → Flink → 数据仓库 | 行为数据流(后期演进) |

---

## 二、核心功能模块详细设计

### 2.1 用户管理模块

**2.1.1 功能清单**

| 功能 | 描述 |
|------|------|
| 注册/登录 | 支持手机号+验证码、微信OAuth2.0扫码登录 |
| 个人信息 | 昵称、头像、母语背景、方言基础等级（自评） |
| 学习档案 | 累计学习时长、连续学习天数、总词汇量、掌握音调数 |
| 等级体系 | 基于潮州方言能力模型（见2.6节）的L1-L8等级 |
| 权限控制 | RBAC模型：学员 / 内容编辑 / 系统管理员 |

**2.1.2 交互流程**

```
注册 → 初始能力测评(10题快测) → 生成初始能力画像 → 推荐起始学习路径
登录 → 加载学习进度 → 恢复上次学习位置 → Agent问候+今日推荐
```

**2.1.3 技术要点**

- JWT双Token机制（Access Token 2h + Refresh Token 7d），Refresh Token存储于Redis并关联设备指纹
- 密码使用bcrypt/argon2哈希，手机号AES-256加密存储
- 微信登录对接微信开放平台，OAuth2.0 authorization_code模式
- 会话管理：Redis集中存储，支持多端登录互踢策略

---

### 2.2 方言学习模块

潮州方言学习模块是整个系统的教学内容载体，围绕"听—说—读—用"四维能力设计。

**2.2.1 发音学习子系统**

| 组件 | 说明 |
|------|------|
| 声母表 | 18个声母，逐母提供标准发音音频、发音部位动画（口腔侧面图/正面图） |
| 韵母表 | 韵母按开口呼/齐齿呼/合口呼分组，包含鼻化韵母、入声韵母 |
| 声调系统 | 8个声调（阴平/阴上/阴去/阴入/阳平/阳上/阳去/阳入），可视化调值曲线（五度标调法） |
| 发音评测 | 用户录音 → ASR转写 → 音素级比对 → 评分(音准/声调/流利度三项) → 纠错反馈 |
| 连续变调训练 | 潮州话特有的前字变调规则教学，2字组→3字组→短句练习 |

**2.2.2 词汇学习子系统**

- 词汇按话题分类：家庭称谓、饮食、数字时间、身体部位、日常活动、民俗文化等12大类
- 每个词条包含：汉字写法、潮州话拼音(潮拼)、国际音标(IPA)、标准发音音频、例句(3句)、配图
- 学习模式：闪卡模式、听音选义、看义拼读、间隔重复(SRS，基于SM-2改进算法)
- 词汇量分级：L1(100词)→L2(300词)→L3(600词)→L4(1000词)→L5(1500词)→L6(2200词)→L7(3000词)→L8(4000+词)

**2.2.3 语法讲解子系统**

- 潮州方言特殊语法点：修饰语后置("人客")、量词用法、比较句("过"字句)、处置式、被动式
- 与普通话对比模式：左右分栏对比展示，差异点高亮标注
- 语法点按难度分级，与学习路径绑定

**2.2.4 对话模拟子系统**

- 场景化对话：菜市场买菜、茶铺喝茶、问路、家庭聚餐、节日民俗等20+生活场景
- 两种模式：
  - **引导模式**：系统给出上句，用户选择/输入下句，逐轮推进
  - **自由模式**：Agent扮演场景角色，与用户进行开放式方言对话
- 对话后复盘：逐句回放，标注发音/语法问题，给出改进建议

---

### 2.3 AI伴学Agent模块（系统核心）

本模块是系统的智能中枢，采用**Agent编排架构**而非单一聊天Bot。

**2.3.1 Agent架构设计**

```
┌─────────────────────────────────────────────┐
│                 Agent 主控循环               │
│                                             │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐ │
│  │ 感知器   │ →  │ 规划器   │ →  │ 执行器   │ │
│  │Perceiver│    │Planner  │    │Executor │ │
│  └────┬────┘    └────┬────┘    └────┬────┘ │
│       │              │              │       │
│       ▼              ▼              ▼       │
│  ┌──────────────────────────────────────┐   │
│  │          工具库 (Toolbox)             │   │
│  │  ┌────────┐┌────────┐┌────────┐     │   │
│  │  │发音评测 ││知识检索 ││内容生成 │ ... │   │
│  │  └────────┘└────────┘└────────┘     │   │
│  └──────────────────────────────────────┘   │
└─────────────────────────────────────────────┘
```

**2.3.2 Agent核心组件**

| 组件 | 职责 | 实现策略 |
|------|------|---------|
| 感知器(Perceiver) | 解析用户输入(文本/语音)、识别意图、提取实体、感知情绪 | 大模型NLU + 意图分类器 + 实体识别模板 |
| 记忆管理器(Memory Manager) | 短期记忆(当前会话上下文)、长期记忆(用户知识掌握图谱、学习偏好、历史对话摘要) | 短期：滑动窗口+摘要压缩；长期：结构化存储于PG + 向量化存入Milvus |
| 规划器(Planner) | 根据用户意图和当前状态，生成执行计划（Task Plan），决定调用哪些工具、以何种顺序 | ReAct范式 / Plan-and-Execute范式，大模型动态规划 |
| 执行器(Executor) | 按规划器输出的步骤序列，逐一调用工具，收集结果，反馈给规划器判断是否需重规划 | 工具调用框架 + 超时/重试机制 |
| 策略引擎(Strategy Engine) | 基于学习策略模型，决定何时推送复习、何时提升难度、何时切换学习模式 | 规则引擎 + 轻量ML模型 |

**2.3.3 Agent能力矩阵**

| 能力类别 | 具体能力 | 触发场景 |
|---------|---------|---------|
| 教学引导 | 新知识讲解、发音示范、纠音指导 | 用户开始新课程 / 发音评分低于阈值 |
| 答疑解惑 | 词汇释义、语法解释、文化背景补充 | 用户提出方言相关问题 |
| 学习建议 | 薄弱点分析报告、学习路径调整建议 | 阶段性评估 / 用户请求反馈 |
| 激励互动 | 成就解锁通知、连续学习鼓励、趣味方言知识推送 | 每日登录 / 里程碑达成 |
| 对话陪练 | 场景角色扮演、自由话题聊天 | 用户选择对话模式 |
| 内容推荐 | 推荐适配当前水平的词汇/语法/场景 | 完成一轮学习后 / 用户闲置 |

**2.3.4 Agent交互流程（以"发音练习纠错"为例）**

```
1. 用户点击"朗读词汇" → 前端录音 → 发送音频至后端
2. 后端ASR引擎转写 → 输出音素序列 + 声调序列
3. Agent感知器接收结果：{词汇ID, 用户音素序列, 声调序列, 标准音素序列, 标准声调序列}
4. Agent规划器判断：音准偏差>阈值 → 生成纠错计划：
   - Step1: 调取该词汇发音部位动画
   - Step2: 高亮播放标准发音
   - Step3: 逐音素对比，定位偏差音素(如:声母[ŋ]发成[n])
   - Step4: 使用大模型生成自然语言纠错指导
   - Step5: 引导用户重新练习(慢速→正常速度)
5. Agent执行器依次调用工具：获取动画→获取音频→音素对比算法→调用大模型生成指导文本
6. 返回前端：动画+音频+对比可视化+纠错指导文本+重新录音按钮
7. 更新用户发音错误记录至知识掌握图谱
```

---

### 2.4 数据分析模块

**2.4.1 分析维度**

| 维度 | 指标 | 计算频率 |
|------|------|---------|
| 学习投入度 | 日/周/月学习时长、登录频率、练习次数 | 实时 + 每日汇总 |
| 学习进度 | 词汇覆盖率、语法点完成率、场景对话完成率 | 实时 |
| 能力水平 | 发音准确率趋势、词汇测试正确率、对话流利度评分 | 每次练习后 |
| 薄弱环节 | 高频错误音素Top10、易混词汇组、未掌握语法点 | 每周汇总 |
| 学习效率 | 单位时间词汇习得量、遗忘曲线拟合参数 | 每周汇总 |

**2.4.2 数据处理管线**

```
原始事件 → [采集层:前端埋点SDK → 后端事件接收API]
         → [缓冲层:Redis Stream / Kafka]
         → [处理层:实时计算(Flink/Spark Streaming) + 批量计算(定时Python任务)]
         → [存储层:统计分析结果写入PG统计表, 原始事件归档至对象存储]
```

**2.4.3 技术要点**

- 前端埋点SDK自研，覆盖页面曝光、点击、录音、答题、对话轮次等20+事件类型
- 实时指标通过Redis Sorted Set + Lua脚本维护滑动窗口计数
- 离线计算使用Python Pandas + 定时Celery任务
- 初期（MVP）可跳过Kafka/Flink，直接用PG + Redis + Celery实现

---

### 2.5 数据可视化模块

**2.5.1 可视化组件清单**

| 组件 | 图表类型 | 数据来源 |
|------|---------|---------|
| 学习日历热力图 | 日历热力图(GitHub贡献图风格) | 每日学习时长 |
| 能力雷达图 | 六维雷达图（发音/词汇/语法/听说/阅读/文化） | 最近一次综合评估 |
| 学习进度趋势 | 折线图(多指标叠加) | 每周汇总数据 |
| 词汇量增长曲线 | 面积图 | 每日词汇掌握量 |
| 声调准确率分布 | 堆叠柱状图(8声调分别统计) | 发音评测记录 |
| 学习时段分布 | 旭日图/饼图 | 学习时间戳聚合 |
| 薄弱项突出卡 | 信息卡片列表（带进度条/预警色） | 薄弱环节分析结果 |
| 学习排行榜 | 排行榜列表（可选社区功能） | 同学段用户学习数据聚合 |

**2.5.2 实现方案**

- 图表库：ECharts(主) + D3.js(高度自定义场景补位)
- 数据获取：前端通过REST API请求聚合后的统计JSON，避免前端大量计算
- 实时更新：学习完成后Socket推送更新通知 → 前端重新拉取对应组件数据
- 导出功能：学习报告支持导出为PDF（前端Canvas渲染 + html2pdf）

---

### 2.6 推理预测模块

**2.6.1 知识掌握度模型**

采用**贝叶斯知识追踪(BKT)改进模型** + **项目反应理论(IRT)** 的混合方案：

| 模型组件 | 用途 |
|---------|------|
| BKT核心 | 对每个知识点(词汇/语法点/音素)维护四元组(P(L₀), P(T), P(G), P(S))，估算学生对该知识点的掌握概率 |
| IRT层 | 对练习题进行参数化（难度b、区分度a、猜测概率c），匹配学生能力θ，实现自适应出题 |
| 遗忘因子 | 在BKT基础上引入艾宾浩斯遗忘曲线修正，P(T)随距上次练习时间动态衰减 |

**2.6.2 学习效果预测**

- **短期预测**：预测用户下次练习的正确率（输入：最近N次练习记录 + 遗忘时间 → 输出：预期正确率区间）
- **长期预测**：预测用户达到下一等级所需天数（输入：当前能力画像 + 目标等级要求 + 历史学习速率 → 输出：预计天数+置信区间）
- **方法选择**：初期使用规则+统计模型（线性回归/指数平滑），数据积累后切换至LightGBM/XGBoost

**2.6.3 潮州方言能力等级模型(L1-L8)**

| 等级 | 词汇量 | 发音准确率 | 语法掌握 | 对话能力 | 水平描述 |
|------|--------|-----------|---------|---------|---------|
| L1 启蒙 | 0-100 | ≥30% | 0-10% | 单字/单词 | 零基础入门 |
| L2 初识 | 100-300 | ≥45% | 10-25% | 简单短语 | 能说日常称谓 |
| L3 入门 | 300-600 | ≥55% | 25-40% | 短句 | 能进行基本问候 |
| L4 初级 | 600-1000 | ≥65% | 40-55% | 简单对话 | 能应对日常生活 |
| L5 中级 | 1000-1500 | ≥75% | 55-70% | 流利对话 | 能与母语者日常交流 |
| L6 进阶 | 1500-2200 | ≥85% | 70-85% | 复杂话题 | 能讨论抽象话题 |
| L7 高级 | 2200-3000 | ≥92% | 85-95% | 深度交流 | 接近母语者 |
| L8 精通 | 3000+ | ≥96% | 95%+ | 母语水平 | 掌握俗语/谚语/文化 |

---

### 2.7 策略辅助模块

**2.7.1 学习路径规划**

- **初始路径生成**：基于入学测评结果，在知识图谱中定位起点，Dijkstra最短路径算法生成到达目标等级的最优路径
- **动态调整**：每完成一个知识单元后，根据掌握度重新评估，必要时插入复习节点或跳过已掌握内容
- **知识图谱结构**：潮州方言知识单元构成有向无环图(DAG)，边表示前置依赖关系（如：声母3→声母3相关词汇→含该声母的短句→对话场景）

**2.7.2 间隔复习调度**

- 算法：SM-2改进版，引入难度自适应
  - 原始SM-2的EF因子扩展为[0.8, 2.5]
  - 对于潮州方言发音类知识，额外乘以声调复杂度系数(声调越难EF调整幅度越大)
- 每日复习队列：按到期时间+优先级排序，晨间推送提醒

**2.7.3 个性化推荐**

| 推荐类型 | 推荐策略 | 触达方式 |
|---------|---------|---------|
| 薄弱点强化包 | 根据近7日错误率Top3薄弱点，自动生成专项练习 | Agent对话推送 |
| 兴趣延伸 | 基于用户高频互动的话题类别，推荐同话题新场景 | 学习中心推荐位 |
| 同伴学习 | 相近水平用户正在学习的内容（脱敏聚合） | 可选社区模块 |
| 文化拓展 | 潮剧选段、潮汕童谣、工夫茶相关方言内容 | 周末特别推送 |

---

### 2.8 内容生成模块

**2.8.1 AI生成能力清单**

| 生成内容类型 | 说明 | 质量控制 |
|------------|------|---------|
| 例句生成 | 给定目标词汇/语法点 → 生成3-5个贴近日常生活的潮州话例句 | 大模型生成 → 规则校验(拼音/声调标注一致性)→ 人工审核入库 |
| 对话脚本生成 | 给定场景+目标词汇范围+难度级别 → 生成8-12轮对话 | 生成 → 大模型自检(检查目标词汇覆盖率)→ 人工抽检 |
| 练习题生成 | 选择题/填空题/听音辨义/连线题，给定知识点+难度 → 生成5题 | 生成 → 自动解析答案验证 → 难度校准(IRT参数估计) |
| 纠错反馈文本 | 给定错误发音详情 → 生成自然语言纠错指导 | 模板+大模型润色，降低幻觉风险 |
| 学习报告摘要 | 给定统计数据 → 生成自然语言的学习评价与建议 | 结构化Prompt + 数据注入 + 大模型生成 |
| 情景扩展 | 已有场景的变体生成（如"菜市场买菜"→ 衍生"超市购物""夜市小吃"） | 大模型改写 → 方言专家评审 |

**2.8.2 内容质量保障机制**

```
大模型生成 → 规则引擎自动校验(格式/拼音/声调一致性/目标覆盖率)
          → 置信度评分
          → [高置信度] → 直接进入内容库(标记"AI生成")
          → [中置信度] → 进入人工审核队列
          → [低置信度] → 丢弃+重试
          → 人工审核通过 → 标记"已审核" → 升级为正式内容
```

---

## 三、数据模型设计

### 3.1 核心实体ER关系

```
┌──────────┐       ┌──────────┐       ┌──────────┐
│   User   │1─────*│LearningRecord│*─────1│KnowledgePoint│
└────┬─────┘       └──────────┘       └─────┬────┘
     │                                      │
     │1                                     │*
┌────┴─────┐                          ┌────┴─────┐
│UserProfile│                          │KnowledgeRelation│
└──────────┘                          └──────────┘
     │                                      │
     │1                                     │*
┌────┴─────┐       ┌──────────┐       ┌────┴─────┐
│AbilityModel│     │LearningPath│*─────│PathNode │
└──────────┘       └──────────┘       └──────────┘
                                             │
                                             │*
┌──────────┐       ┌──────────┐       ┌────┴─────┐
│Vocabulary│*─────*│  Lesson  │       │Exercise  │
└──────────┘       └──────────┘       └──────────┘
     │                                      │
     │1                                     │1
┌────┴─────┐       ┌──────────┐       ┌────┴─────┐
│ AudioFile│       │DialogueScene│     │ExerciseRecord│
└──────────┘       └──────────┘       └──────────┘
```

### 3.2 核心数据表定义

**3.2.1 用户与档案**

```sql
-- 用户基础表
users (
    id              UUID PK,
    phone           VARCHAR(20) UNIQUE,        -- AES加密存储
    wechat_unionid  VARCHAR(64) UNIQUE,
    nickname        VARCHAR(50),
    avatar_url      VARCHAR(500),
    password_hash   VARCHAR(256),              -- argon2
    created_at      TIMESTAMP,
    updated_at      TIMESTAMP
)

-- 用户学习档案(1:1 users)
user_profiles (
    id              UUID PK,
    user_id         UUID FK → users.id UNIQUE,
    native_language VARCHAR(30),               -- 母语/方言背景
    self_eval_level INTEGER CHECK(1-8),        -- 自评等级
    current_level   INTEGER CHECK(1-8),        -- 系统评定等级
    total_study_sec INTEGER DEFAULT 0,         -- 累计学习秒数
    streak_days     INTEGER DEFAULT 0,         -- 连续学习天数
    last_study_date DATE,
    vocab_mastered  INTEGER DEFAULT 0,         -- 已掌握词汇数
    tone_accuracy   DECIMAL(5,2),              -- 声调综合准确率
    created_at      TIMESTAMP,
    updated_at      TIMESTAMP
)

-- 能力模型快照(定期更新)
ability_snapshots (
    id              UUID PK,
    user_id         UUID FK → users.id,
    pronunciation   DECIMAL(5,2),  -- 发音维度
    vocabulary      DECIMAL(5,2),  -- 词汇维度
    grammar         DECIMAL(5,2),  -- 语法维度
    listening       DECIMAL(5,2),  -- 听力维度
    speaking        DECIMAL(5,2),  -- 口语维度
    culture         DECIMAL(5,2),  -- 文化维度
    overall_score   DECIMAL(5,2),
    evaluated_at    TIMESTAMP
)
```

**3.2.2 学习内容**

```sql
-- 知识点表
knowledge_points (
    id              UUID PK,
    kp_type         VARCHAR(20),    -- 'initial'/'final'/'tone'/'vocab'/'grammar'/'sentence'
    title           VARCHAR(100),
    description     TEXT,
    level           INTEGER CHECK(1-8),
    pinyin          VARCHAR(50),    -- 潮拼
    ipa             VARCHAR(50),    -- 国际音标
    hanzi           VARCHAR(200),   -- 对应汉字
    metadata        JSONB,          -- 扩展属性(声母发音部位、声调调值、词汇词性等)
    created_at      TIMESTAMP
)

-- 知识依赖关系(DAG边)
knowledge_relations (
    id              UUID PK,
    predecessor_id  UUID FK → knowledge_points.id,
    successor_id    UUID FK → knowledge_points.id,
    relation_type   VARCHAR(20),    -- 'prerequisite'/'related'
    UNIQUE(predecessor_id, successor_id)
)

-- 词汇表(知识点子类型)
vocabularies (
    id              UUID PK,
    kp_id           UUID FK → knowledge_points.id UNIQUE,
    word_category   VARCHAR(30),    -- 话题分类
    difficulty      INTEGER CHECK(1-5),
    example_sentences JSONB,        -- [{pinyin, ipa, hanzi, translation},...]
    image_url       VARCHAR(500),
    created_at      TIMESTAMP
)

-- 课程/课时
lessons (
    id              UUID PK,
    title           VARCHAR(100),
    description     TEXT,
    level           INTEGER CHECK(1-8),
    kp_ids          UUID[],         -- 包含的知识点ID列表
    lesson_type     VARCHAR(20),    -- 'pronunciation'/'vocab'/'grammar'/'dialogue'/'comprehensive'
    duration_est    INTEGER,        -- 预计学习时长(秒)
    order_index     INTEGER,
    created_at      TIMESTAMP
)

-- 对话场景
dialogue_scenes (
    id              UUID PK,
    title           VARCHAR(100),
    scene_category  VARCHAR(30),    -- 'market'/'tea_house'/'family'/'festival'等
    level           INTEGER CHECK(1-8),
    target_vocabs   UUID[],         -- 目标词汇
    target_grammar  UUID[],         -- 目标语法点
    dialogues       JSONB,          -- [{role, pinyin, ipa, hanzi, translation},...]
    mode            VARCHAR(20),    -- 'guided'/'free'
    created_at      TIMESTAMP
)
```

**3.2.3 学习记录**

```sql
-- 学习事件记录(核心行为表)
learning_records (
    id              UUID PK,
    user_id         UUID FK → users.id,
    record_type     VARCHAR(30),    -- 'pronunciation_practice'/'vocab_quiz'/'dialogue'/'lesson_complete'
    target_type     VARCHAR(20),    -- 'kp'/'lesson'/'scene'
    target_id       UUID,
    score           DECIMAL(5,2),   -- 得分(如有)
    duration_sec    INTEGER,        -- 耗时
    detail          JSONB,          -- 详细数据(发音评测维度分、答题详情等)
    device_info     JSONB,
    created_at      TIMESTAMP
)
-- 索引: (user_id, created_at DESC), (user_id, record_type, created_at)

-- 知识掌握状态(每个用户-每个知识点的BKT状态)
knowledge_mastery (
    id              UUID PK,
    user_id         UUID FK → users.id,
    kp_id           UUID FK → knowledge_points.id,
    p_learned       DECIMAL(5,4),   -- BKT P(L) 掌握概率
    p_transit       DECIMAL(5,4),   -- BKT P(T) 学习转移概率
    p_guess         DECIMAL(5,4),   -- BKT P(G) 猜测概率
    p_slip          DECIMAL(5,4),   -- BKT P(S) 失误概率
    last_practice_at TIMESTAMP,
    practice_count  INTEGER DEFAULT 0,
    consecutive_correct INTEGER DEFAULT 0,
    ef_factor       DECIMAL(4,2) DEFAULT 2.5,  -- SM-2 EF因子
    next_review_at  TIMESTAMP,
    updated_at      TIMESTAMP
)
-- 唯一约束: (user_id, kp_id)
```

**3.2.4 Agent相关**

```sql
-- Agent对话会话
agent_sessions (
    id              UUID PK,
    user_id         UUID FK → users.id,
    session_type    VARCHAR(30),    -- 'free_chat'/'guided_practice'/'q_and_a'/'review'
    context_ref     JSONB,          -- 关联的学习上下文 {lesson_id, scene_id, kp_ids}
    status          VARCHAR(20) DEFAULT 'active',  -- 'active'/'closed'
    summary         TEXT,           -- 会话总结(AI生成)
    created_at      TIMESTAMP,
    closed_at       TIMESTAMP
)

-- 对话消息
agent_messages (
    id              UUID PK,
    session_id      UUID FK → agent_sessions.id,
    role            VARCHAR(10),    -- 'user' / 'agent'
    content_type    VARCHAR(20),    -- 'text' / 'audio' / 'tool_call' / 'tool_result'
    content         TEXT,           -- 文本内容或JSON
    audio_url       VARCHAR(500),   -- 语音消息音频地址
    tool_name       VARCHAR(50),    -- 调用的工具名(如有)
    tool_params     JSONB,          -- 工具参数
    tokens_used     INTEGER,        -- Token消耗
    latency_ms      INTEGER,        -- 响应延迟
    created_at      TIMESTAMP
)
```

---

## 四、API接口规范

### 4.1 接口设计原则

- RESTful风格，JSON格式数据交换，UTF-8编码
- 版本化：URL前缀 `/api/v1/`
- 认证：Authorization: Bearer `<JWT>`
- 分页：统一使用 `?page=1&page_size=20`，响应含 `total`/`page`/`page_size`/`total_pages`
- 错误响应统一格式：`{"code": "ERROR_CODE", "message": "中文描述", "details": {...}}`

### 4.2 核心接口列表

#### 4.2.1 用户服务 `/api/v1/users`

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/auth/register` | 手机号注册 |
| POST | `/auth/login` | 手机号/微信登录 |
| POST | `/auth/refresh` | 刷新Token |
| GET | `/me/profile` | 获取个人信息 |
| PUT | `/me/profile` | 更新个人信息 |
| GET | `/me/progress` | 获取学习进度总览 |
| GET | `/me/ability` | 获取最新能力模型 |

#### 4.2.2 学习服务 `/api/v1/learning`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/lessons` | 课程列表(可按等级/类型筛选) |
| GET | `/lessons/{id}` | 课程详情(含关联知识点) |
| GET | `/knowledge-points` | 知识点列表(可按等级/类型/搜索关键词) |
| GET | `/knowledge-points/{id}` | 知识点详情(含音频/动画资源URL) |
| GET | `/path/my` | 获取个人学习路径(含完成状态) |
| POST | `/records` | 提交学习记录 |
| GET | `/records?type=&from=&to=` | 查询学习记录 |
| POST | `/vocab/{id}/practice` | 提交词汇练习结果 |
| GET | `/review-queue` | 获取待复习队列 |

#### 4.2.3 Agent服务 `/api/v1/agent`

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/sessions` | 创建Agent会话 |
| GET | `/sessions` | 获取会话列表 |
| GET | `/sessions/{id}` | 获取会话详情(含消息历史) |
| POST | `/sessions/{id}/messages` | 发送消息(文本) |
| POST | `/sessions/{id}/audio` | 发送语音消息(multipart/form-data, 音频文件) |
| WSS | `/ws/agent/{session_id}` | WebSocket实时对话通道 |
| POST | `/sessions/{id}/close` | 关闭会话(触发生成总结) |

#### 4.2.4 数据分析服务 `/api/v1/analytics`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/dashboard/summary` | 个人数据概览 |
| GET | `/dashboard/heatmap?month=` | 学习日历热力图数据 |
| GET | `/dashboard/radar` | 能力雷达图数据 |
| GET | `/dashboard/trend?metric=&period=` | 趋势数据(周/月) |
| GET | `/dashboard/weakness` | 薄弱环节TopN |
| GET | `/report/generate?period=weekly` | 生成学习报告(PDF下载链接) |

#### 4.2.5 内容管理服务 `/api/v1/admin` (管理员)

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/knowledge-points` | 创建知识点 |
| PUT | `/knowledge-points/{id}` | 更新知识点 |
| DELETE | `/knowledge-points/{id}` | 删除知识点 |
| POST | `/lessons` | 创建课程 |
| POST | `/scenes` | 创建对话场景 |
| POST | `/content/generate` | 触发AI内容生成 |
| GET | `/content/review-queue` | 获取待审核AI生成内容 |
| POST | `/content/{id}/approve` | 审核通过 |
| POST | `/content/{id}/reject` | 审核驳回 |

### 4.3 AI模型调用接口规范（内部）

```json
{
  "model": "deepseek-chat | qwen-turbo | glm-4",
  "messages": [
    {"role": "system", "content": "你是潮州方言教学Agent..."},
    {"role": "user", "content": "用户输入"}
  ],
  "temperature": 0.7,
  "max_tokens": 2048,
  "tools": [
    {
      "type": "function",
      "function": {
        "name": "evaluate_pronunciation",
        "description": "评测发音准确度",
        "parameters": {...}
      }
    }
  ],
  "tool_choice": "auto"
}
```

### 4.4 WebSocket消息协议

```json
// 客户端 → 服务端
{
  "type": "message | audio | ping",
  "session_id": "uuid",
  "payload": {
    "text": "...",           // type=message 时
    "audio_base64": "...",   // type=audio 时
    "context": {}            // 可选:前端状态快照
  }
}

// 服务端 → 客户端
{
  "type": "text | audio | tool_call | error | thinking | session_summary",
  "message_id": "uuid",
  "payload": {
    "text": "...",
    "audio_url": "...",
    "tool_name": "...",
    "tool_result": {},
    "thinking": "..."       // Agent思考过程(可选展示)
  },
  "timestamp": "ISO8601"
}
```

---

## 五、技术选型建议

### 5.1 前端技术栈

| 层次 | 推荐方案 | 备选方案 | 选型理由 |
|------|---------|---------|---------|
| 框架 | **Svelte 4/5** | React 18 + Next.js | 编译时框架，产物体积小，无虚拟DOM开销，适合教育类重交互场景；学习曲线平缓 |
| UI组件库 | **Tailwind CSS + DaisyUI** | Ant Design / Naive UI | 原子化CSS，定制灵活；DaisyUI提供语义化组件；不依赖特定框架 |
| 图表 | **ECharts 5** | Chart.js / D3.js | 国产成熟方案，中文文档完善，交互丰富，移动端适配好 |
| 音频处理 | **Web Audio API + Recorder.js** | MediaRecorder API | 精细控制音频采集参数(采样率16kHz/单声道)，支持WAV/PCM格式 |
| 状态管理 | **Svelte stores** | Zustand (React方案) | Svelte内置响应式stores，无需额外依赖 |
| 路由 | **SvelteKit routing** | React Router | 基于文件系统的路由，SSR/CSR灵活切换 |
| 构建 | **Vite** | Webpack | 开发服务器毫秒级热更新，构建速度快 |

### 5.2 后端技术栈

| 层次 | 推荐方案 | 备选方案 | 选型理由 |
|------|---------|---------|---------|
| 主语言 | **Python 3.11+** | Node.js(TypeScript) / Go | AI/ML生态最完善，数据处理库丰富(发音分析/模型推理)；FastAPI异步性能足够 |
| Web框架 | **FastAPI** | Django Ninja / Flask | 原生异步，自动OpenAPI文档，类型安全，WebSocket支持好 |
| ORM | **SQLAlchemy 2.0 (async)** | Prisma / Tortoise-ORM | Python ORM事实标准，2.0异步支持成熟 |
| 任务队列 | **Celery + Redis** | ARQ / Dramatiq | 生态成熟，支持定时任务(复习提醒/报告生成) |
| 实时通信 | **Socket.IO (python-socketio)** | 原生WebSocket | 房间管理、自动重连、降级轮询，前端库配套完善 |
| API网关 | **Nginx(反向代理) + FastAPI中间件** | Kong / Traefik | 前期简单够用，后期可升级为Kong |
| 日志/监控 | **Loguru + Prometheus + Grafana** | ELK | 轻量级，Prometheus生态标准 |

### 5.3 数据存储方案

| 数据类型 | 存储方案 | 说明 |
|---------|---------|------|
| 业务数据 | **PostgreSQL 15** | 用户、课程、学习记录、Agent会话等结构化数据；支持JSONB灵活扩展 |
| 缓存/Session | **Redis 7** | JWT黑名单、Session、排行榜、限流、Celery Broker、实时计数 |
| 音频/媒体文件 | **MinIO** (自建) / **阿里云OSS** | S3兼容对象存储，音频文件URL化存储 |
| 知识向量库 | **Milvus Lite**(初期) → **Milvus Cluster**(规模化) | 方言知识库语义检索、相似例句搜索、对话场景匹配 |
| 搜索引擎 | **Elasticsearch** (后期) | 全文搜索学习内容、对话脚本检索 |
| 行为事件(后期) | **Kafka + ClickHouse** | 大规模学习行为事件流处理与分析 |

### 5.4 国产大模型接入方案

| 模型 | 调用方式 | 适用场景 | 优先级 |
|------|---------|---------|--------|
| **DeepSeek-V3** | DeepSeek API / 私有化部署 | Agent核心推理(ReAct)、复杂指令遵循、内容生成 | ⭐⭐⭐ 主力 |
| **通义千问(Qwen-Max)** | 阿里云DashScope API | 长文本生成(对话脚本、学习报告)、多模态理解(OCR潮州话文本) | ⭐⭐⭐ 主力 |
| **智谱GLM-4** | 智谱AI开放平台API | Agent工具调用、Function Call场景 | ⭐⭐ 辅助 |
| **Kimi(Moonshot)** | Moonshot API | 超长上下文(128k)的知识检索问答 | ⭐⭐ 辅助 |
| **讯飞星火** | 讯飞开放平台 | 语音识别(潮州话ASR定制)、语音合成(TTS) | ⭐⭐⭐ 语音专项 |

### 5.5 模型路由器设计

模型路由器是AI集成层的核心组件，负责请求分发、故障切换与成本控制。

```
请求进入 → 意图分类 → 路由决策 → 模型调用 → 结果校验 → 返回
                │                    │
                ▼                    ▼
           ┌──────────┐       ┌──────────┐
           │ 路由规则表 │       │ 故障转移队列│
           │(可热更新) │       │(熔断/降级) │
           └──────────┘       └──────────┘
```

| 路由维度 | 规则 |
|---------|------|
| 意图路由 | Agent推理→DeepSeek, 长文本生成→Qwen, FunctionCall→GLM-4, 超长上下文→Kimi |
| 成本路由 | 简单任务(纠错文本生成)→DeepSeek(低成本), 复杂任务(对话脚本全量生成)→Qwen-Max |
| 负载路由 | 主模型QPS超阈值→自动切换到备选模型 |
| 质量路由 | 主模型返回结果置信度低于阈值→用备选模型重新生成→择优返回 |

故障切换策略：熔断器模式（连续失败5次→熔断30s→半开探测→恢复/继续熔断），结合指数退避重试。

### 5.6 Agent工具库定义

Agent通过Function Call机制调用的工具集：

| 工具名 | 功能 | 输入 | 输出 |
|--------|------|------|------|
| `search_knowledge` | 语义检索方言知识库 | 查询文本 | 相关知识条目+相似度 |
| `get_lesson_content` | 获取课程/知识点详情 | 课程ID/知识点ID | 完整内容+资源URL |
| `evaluate_pronunciation` | 评测发音 | 音频URL+标准音素 | 评分+偏差分析 |
| `get_user_progress` | 获取用户学习状态 | 用户ID+维度 | 掌握度+薄弱点 |
| `generate_exercise` | 生成针对性练习 | 知识点ID+难度+数量 | 练习题JSON |
| `get_review_queue` | 获取待复习项 | 用户ID+数量 | 复习队列 |
| `search_dialogue_scene` | 匹配对话场景 | 场景描述/关键词 | 场景列表 |
| `log_learning_event` | 记录学习事件 | 事件类型+详情 | 确认 |

---

## 六、数据安全措施

### 6.1 数据分级保护

| 安全等级 | 数据类别 | 保护措施 |
|---------|---------|---------|
| **L3 高敏感** | 用户手机号、密码哈希、微信UnionID | AES-256-GCM加密存储、传输层TLS 1.3、日志脱敏、数据库字段级加密 |
| **L2 中敏感** | 学习记录、Agent对话内容、语音录音 | 传输加密、存储访问控制、7天自动清理原始音频、对话内容脱敏后可用于模型训练 |
| **L1 一般** | 课程内容、公开知识点、学习统计数据 | 传输加密、防篡改签名、CDN缓存 |

### 6.2 安全架构措施

| 层面 | 措施 |
|------|------|
| 传输安全 | 全站HTTPS(TLS 1.3)，WebSocket使用WSS；HSTS头部强制 |
| 认证安全 | JWT签名(RSA256)、Refresh Token绑定设备指纹、登录失败锁定(5次/15min) |
| API安全 | 接口限流(Token Bucket, 100req/min per user)、参数校验(Pydantic Schema)、SQL注入防护(SQLAlchemy参数化查询) |
| 数据存储 | 数据库连接加密(SSL)、敏感字段应用层加密(PG pgcrypto + 应用层AES)、定期备份(每日全量+每小时增量) |
| 访问控制 | RBAC三级角色、API端点权限装饰器、数据库行级安全策略(RLS) |
| 审计日志 | 关键操作(登录/修改密码/内容审核/数据导出)记录审计日志，不可篡改 |
| 依赖安全 | 定期`pip audit` / `npm audit`扫描，CI/CD中集成Snyk/Trivy |
| 内容安全 | 用户生成内容(UGC语音/文本)接入内容审核服务(阿里云/网易易盾)，敏感词过滤 |

### 6.3 隐私合规

- 遵循《个人信息保护法》，用户数据收集最小化原则
- 语音数据默认本地处理（前端降噪、VAD），仅上传必要片段
- 隐私政策明确告知数据用途，提供数据导出与删除功能
- 未成年人模式：14岁以下用户限制录音分享、关闭社区功能(如有)

---

## 七、性能优化策略

### 7.1 前端优化

| 优化项 | 策略 | 目标指标 |
|--------|------|---------|
| 首屏加载 | SvelteKit代码分割(路由级lazy loading)、关键CSS内联、字体子集化 | FCP < 1.5s |
| 静态资源 | 音频文件CDN分发(阿里云CDN)、图片WebP格式+LQIP占位、Gzip/Brotli压缩 | 资源体积减少60%+ |
| 音频加载 | 预加载当前课程+下一课音频、AudioContext复用池 | 音频播放延迟 < 200ms |
| 渲染优化 | 图表组件按需加载、学习记录列表虚拟滚动(Virtual List) | 长列表滚动 60fps |
| 离线能力 | Service Worker缓存静态资源+最近3课内容，弱网/离线可浏览已缓存内容 | 离线基本可用 |
| 埋点上报 | 使用`navigator.sendBeacon`异步上报，不阻塞主线程 | 对交互零影响 |

### 7.2 后端优化

| 优化项 | 策略 | 目标指标 |
|--------|------|---------|
| 数据库查询 | 关键查询添加联合索引、N+1问题使用`selectinload`预加载、频繁查询结果Redis缓存(Cache-Aside) | P99查询 < 100ms |
| API响应 | 列表接口分页(默认20条)、大字段懒加载(GET列表不含detail JSONB)、gzip压缩 | P99响应 < 500ms |
| Agent响应 | 流式输出(SSE/WebSocket分块推送)、首Token延迟优化(模型预热连接池) | 首Token < 2s |
| 并发控制 | uvicorn多worker + gunicorn进程管理、数据库连接池(SQLAlchemy pool_size=20) | 并发500 QPS |
| 异步任务 | 学习记录写入、统计更新、报告生成全部异步化(Celery) | API同步响应 < 300ms |
| 音频处理 | 音频分析(ASR/音素比对)使用独立Worker进程池，避免阻塞事件循环 | 音频分析 < 3s |

### 7.3 AI调用优化

| 优化项 | 策略 |
|--------|------|
| 连接池 | 维护与各模型API的HTTP/2长连接池，复用TCP连接，减少握手开销 |
| Prompt缓存 | 系统Prompt(潮州方言教学System Prompt)通过模型厂商的Prompt Caching机制缓存(DeepSeek支持)，降低首Token延迟与费用 |
| 并发控制 | 模型API调用使用信号量限制并发数，避免超出配额 |
| 结果缓存 | 相同输入的内容生成请求(Lesson简介、固定场景对话)缓存结果24h |
| 流式输出 | 所有Agent对话使用Stream模式，边生成边推送，提升感知速度 |
| Token预算 | 每会话每用户设置Token消耗上限(如5000 Token/会话)，超出后引导用户进入总结模式 |

---

## 八、扩展性设计

### 8.1 多方言支持扩展

系统架构预留方言抽象层，核心设计模式为**方言插件化**：

```
方言抽象接口 (DialectPlugin)
├── phonology:     音系定义(声母表/韵母表/声调表)
├── romanization:  拼音方案(罗马化规则)
├── tone_sandhi:   连读变调规则
├── grammar_rules: 特有语法规则
└── cultural_notes: 文化背景知识

潮州话插件  → 实现 DialectPlugin
客家话插件  → 实现 DialectPlugin
粤语插件    → 实现 DialectPlugin
...
```

具体扩展点：

| 扩展层面 | 当前实现 | 多方言扩展方式 |
|---------|---------|---------------|
| 发音评测 | 潮州话音素模型 | 新增音素模型文件 + 方言插件注册 |
| 知识图谱 | 潮州话知识DAG | 每方言独立知识图谱，共享学习策略引擎 |
| AI Prompt | 潮州话System Prompt | 动态System Prompt模板，根据当前方言注入对应语言规则 |
| 内容库 | 潮州话词汇/场景/语法 | 方言独立内容命名空间，后台按方言切换 |
| 前端UI | 潮州话专属界面 | 语言切换器 + 方言专属视觉主题(颜色/图标) |

### 8.2 AI能力扩展架构

AI能力采用**适配器模式** + **配置驱动**实现模型灵活切换：

```python
# 模型适配器抽象
class BaseModelAdapter(ABC):
    @abstractmethod
    async def chat(self, messages, tools=None, stream=False): ...
    @abstractmethod
    async def embed(self, texts): ...

class DeepSeekAdapter(BaseModelAdapter): ...   # DeepSeek API适配
class QwenAdapter(BaseModelAdapter): ...       # 通义千问适配
class GLMAdapter(BaseModelAdapter): ...        # 智谱GLM适配

# 模型路由器通过配置切换
model_config = {
    "router_rules": [
        {"intent": "agent_reasoning", "primary": "deepseek", "fallback": "glm"},
        {"intent": "long_text_gen",  "primary": "qwen",     "fallback": "deepseek"},
        {"intent": "function_call",  "primary": "glm",      "fallback": "deepseek"},
    ],
    "adapters": {
        "deepseek": {"class": "DeepSeekAdapter", "api_key_env": "DEEPSEEK_KEY", ...},
        "qwen":     {"class": "QwenAdapter",     "api_key_env": "QWEN_KEY", ...},
        "glm":      {"class": "GLMAdapter",      "api_key_env": "GLM_KEY", ...},
    }
}
```

新增模型只需：实现BaseModelAdapter → 注册到配置 → 更新路由规则，无需修改业务代码。

### 8.3 社区互动扩展

预留扩展接口（MVP不实现，架构预留）：

| 模块 | 预留设计 |
|------|---------|
| 学习小组 | `groups`表 + `group_members`表 + `group_leaderboard`视图，用户可创建/加入学习小组 |
| 方言交流广场 | 类似论坛的帖子/评论模型，`posts` + `comments`表，需配套内容审核 |
| 学习打卡 | `check_in_records`表，配合SNS分享接口(微信JS-SDK) |
| 方言配音秀 | 短视频配音功能，预留`dubbing_tasks`表 + 视频合成服务接口 |

### 8.4 移动端适配扩展

| 层面 | 策略 |
|------|------|
| 响应式布局 | Tailwind CSS响应式断点(sm/md/lg/xl)，优先移动端设计(Mobile First) |
| PWA | Service Worker + Web App Manifest，支持添加到主屏幕、离线缓存、推送通知 |
| 原生封装(远期) | 预留API版本号 + 客户端类型头(`X-Client-Type: web/ios/android`)，原生App通过API对接 |
| 微信小程序 | API层完全可复用于小程序后端，前端需独立开发(小程序WXML/WXSS)，架构上独立部署前端壳 |

### 8.5 数据规模演进

| 阶段 | 用户规模 | 技术方案 |
|------|---------|---------|
| **MVP(0-1k)** | < 1000 | 单机部署：FastAPI + PG + Redis + Milvus Lite 全部单机，Docker Compose编排 |
| **成长期(1k-10k)** | 1000-10000 | 服务拆分：PG读写分离、Redis Cluster、MinIO分布式、音频处理独立服务 |
| **规模化(10k-100k)** | 10000-100000 | 微服务化：K8s编排、Kafka事件总线、ClickHouse分析、模型私有化部署(GPU集群) |
| **平台化(100k+)** | 100000+ | 多机房部署、自建CDN、AI模型自训练微调、数据中台 |

---

## 九、项目里程碑建议

| 阶段 | 周期 | 核心交付 |
|------|------|---------|
| **P0: 基础框架** | 第1-4周 | 项目脚手架搭建、用户模块(注册/登录/档案)、管理后台基础CRUD |
| **P1: 内容建设** | 第3-8周 | 潮州方言知识图谱构建(声母/韵母/声调/基础500词/50语法点)、标准音频录制 |
| **P2: 学习核心** | 第5-12周 | 发音学习模块、词汇闪卡+测验、发音评测(ASR+音素比对)、学习记录 |
| **P3: Agent集成** | 第9-16周 | Agent编排引擎、意图识别+工具调用、对话模拟(引导模式)、内容生成管道 |
| **P4: 智能增强** | 第13-20周 | BKT知识追踪、学习路径规划、数据可视化看板、学习报告生成、间隔复习 |
| **P5: 体验打磨** | 第17-24周 | 对话模拟(自由模式)、性能优化、安全加固、内测与迭代 |

---

## 十、风险与应对

| 风险 | 影响 | 应对策略 |
|------|------|---------|
| 潮州方言语料稀缺，大模型方言能力不足 | 内容生成质量不达预期 | 构建高质量Few-shot示例库 + 方言知识RAG增强 + 人工审核兜底 |
| ASR对潮州话识别准确率低 | 发音评测功能失效 | 优先使用讯飞方言定制模型 + 自训练小型潮州话ASR模型(Whisper微调) |
| 国产大模型API不稳定/涨价 | 服务中断/成本超预算 | 多模型路由+故障切换 + 自部署开源模型(DeepSeek/Qwen开源版)作为兜底 |
| 学习内容制作成本高 | 内容更新慢 | AI辅助生成 + 社区贡献(UGC)机制 + 模板化内容生产 |
| 用户留存率低 | 产品失败 | Agent驱动的个性化体验 + 游戏化设计(成就/等级/连续打卡) + 微信社交裂变 |

---

*文档版本 v1.0 · 2026年5月*

